import { HttpHeaders } from '@angular/common/http';

export const headers = new HttpHeaders({ "Content-Type": "application/json" });
export const headersPlain = new HttpHeaders({ "Content-Type": "text/plain" });